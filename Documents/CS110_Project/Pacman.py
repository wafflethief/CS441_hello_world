import Controller
import Wall
import pygame
import turtle
import Controller
import Cookies

class Pacman:
	def __init__(self, x, y, img_file):
		pygame.init()
		self.xcoord = x
		self.ycoord = y
		direction = ''
		hitGhost = False
		curPoints = 0
		isMoving = False
	
	def turn(self, direction):
		if direction #is it faster to put all the l, r, u, d movements in one function?
	def move(self):
		
	def die(self):
		